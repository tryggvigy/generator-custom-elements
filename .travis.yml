language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-custom-elements' ]; then cd .. && eval "mv $currentfolder generator-custom-elements" && cd generator-custom-elements; fi
